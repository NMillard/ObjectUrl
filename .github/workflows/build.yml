name: Build   

on:
  push:
    branches:
      - '*'
    paths-ignore:
      - 'examples/*'
jobs:
  build:
    uses: NMillard/ObjectUrl/.github/workflows/build-base-template.yml@main
    with:
      dotnet-version: '7.0.x'